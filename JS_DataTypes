//Lec5e

"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser
//js engine is inside the browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;


//primirive datatype
// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value ..khali value
// undefined => variable is declared but the value is not assigned to it
// symbol => unique  //used in react to find uniqueness


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
